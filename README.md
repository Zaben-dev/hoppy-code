Blog application built with an Nx monorepo, Angular frontend, and NestJS backend.
There is a shared package with DTOs used by both the backend and the frontend.
Nest uses Prisma for database access.
It also uses cursor pagination — which is, of course, overkill for a small blog, but it’s just a fun side project 🙂
To be continued…
